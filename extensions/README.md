Place the unpacked Prompt Security extension under `extensions/prompt-security`.

Expected structure:

```text
extensions/
  prompt-security/
    manifest.json
    ...
```

CI can also materialize this directory from the `PROMPT_SECURITY_EXTENSION_ZIP_BASE64` secret.
